[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    legacy: legacy Railway/API checks that are opt-in
    integration: tests that call a running local API
